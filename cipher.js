const cipher = {

    //  decifrar 
    decode: function (offsetGuardado, mensajeGuardado) {
        console.log(offsetGuardado, mensajeGuardado);
    },

    //cifrar
    encode: function (offsetGuardado, mensajeGuardado) {
        console.log(offsetGuardado, mensajeGuardado);

    }
}

export default cipher;
